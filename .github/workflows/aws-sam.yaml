name: Reusable workflow for AWS SAM
on:
  workflow_call:
    inputs:
      PYTHON_VERSION:
        required: true
        type: string
      PROJECT_FOLDER:
        required: true
        type: string



jobs:
  requirements:
    runs-on: ubuntu-latest
    name: Requirements
    environment:  auto
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: ${{ inputs.PROJECT_FOLDER }}/tests/requirements.txt
      - run: pip install -r ${{ inputs.PROJECT_FOLDER }}/tests/requirements.txt
      - run: coverage --help
      - name: SAM Validate
        run: |
          sam validate
        working-directory: ${{ inputs.PROJECT_FOLDER }}

  build:
    runs-on: ubuntu-latest
    name: Build
    environment:  auto
    needs: requirements
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.PYTHON_VERSION }}
          cache: 'pip'
      - name: SAM Build
        run: |
          sam build
        working-directory: ${{ inputs.PROJECT_FOLDER }}
      - name: Run unit tests
        run: |
          coverage run -m pytest -v  route-53-automation/tests/unit
        working-directory: ${{ inputs.PROJECT_FOLDER }}
      - name: Coverage Report
        run: coverage report -m